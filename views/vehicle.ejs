<!DOCTYPE html>
<html>

<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <p>This is the vehicle page</p>
    <select name="make">
        <option value="select make">select make</option>
        <% makes.forEach(make => { %>
        <option value="<%=make.data.id%>"><%=make.data.attributes.name%></option>
        <% }) %>
    </select><br>
    <select name="model">
        <option value="select make">select model</option>
        <% if (models) { models.forEach(model => { %>
            <option value="<%=models.data.id%>"><%=models.data.attributes.name%></option>
        <% })} else { %>
            
        <% } %>
    </select><br>
    <input value="year"></input><br>
    <input value="amount"></input>
    <select name="unit">
        <option value="mi">mi</option>
        <option value="km">km</option>
    </select>
</body>

</html>